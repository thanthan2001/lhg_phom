//import 'package:epose_app/core/data/pref/prefs';
//import 'package:epose_app/core/data/pref/prefs';
import 'package:get/get.dart';

class AppBinding extends Bindings {
  @override
  void dependencies() async {
    // final Prefs prefs = Prefs();
    // final GetLocationCase getLocationCase = GetLocationCase(prefs);
    // final SaveLoactionCase saveLocationCase = SaveLoactionCase(prefs);
    // final locationService =
    //     LocationService(getLocationCase, saveLocationCase, prefs);

    // Get.put(getLocationCase);
    // Get.put(locationService);

    // // Gọi phương thức initializeLocation để lấy vị trí hiện tại khi khởi động app
    // await locationService.initializeLocation();
  }
}